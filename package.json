{
  "name": "ng-file-uploader",
  "version": "1.0.0",
  "description": "app for angular file uploader",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "postinstall": "bower cache clean && bower install"
  },
  "author": "shreedharshetty",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.3.2",
    "express": "^4.14.0",
    "pug": "^0.1.0"
  }
}
